extends layout

block content
  h1= title
  - let engines = results
  //- var results = [{"bikesType":"2 wheeler","Cylinders":0,"FuelType":"no fuel","Transmission":"No gears"},{"bikes type":"hiking bikes","Cylinders":0,"FuelType":"no fuel","Transmission":"No gears"},{"bikes types":"Radial","Cylinders":0,"FuelType":"LPG","Transmission":"Automatic"}];
  div(class="container-fluid")
    div(class="row")
    each item, index in engines
      div(class="col-sm-2" width="215px", onClick=`buy(${index})`) bikes type:#{item.bikesType} <br> Cylinders:#{item.Cylinders} <br> FuelType:#{item.FuelType} <br> Transmission:#{item.Transmission}
  
  script. 
    function buy(index) {
      alert(`You purchased item ${index}`);
    }